import React from 'react';
import ReactDOM from 'react-dom/client';
import Stats from './subwindows/Stats';

ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    <Stats />
  </React.StrictMode>
);
